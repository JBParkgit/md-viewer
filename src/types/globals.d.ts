// Build-time constants injected by electron-vite's `define` (see
// electron.vite.config.ts). Centralizing here keeps UI code in sync with
// package.json without manual edits each release.
declare const __APP_VERSION__: string
